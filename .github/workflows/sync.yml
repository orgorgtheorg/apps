name: Sync catalog

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Validate manifests
        run: node scripts/sync.mjs --check

      - name: Sync catalog → dev
        env:
          CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_DEPLOY_KEY_DEV }}
        run: node scripts/sync.mjs

      # Runs only once the CONVEX_DEPLOY_KEY_PROD secret exists — the script
      # exits 0 with a "skipping" note when the key is absent.
      - name: Sync catalog → prod
        env:
          CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_DEPLOY_KEY_PROD }}
        run: node scripts/sync.mjs
